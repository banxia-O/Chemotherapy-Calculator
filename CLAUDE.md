# 化疗剂量计算器 — 项目约定

微信小程序：肿瘤化疗剂量计算器。临床参考工具，使用者为肿瘤科医生。
默认用**中文**回复。

> 📌 **维护约定**：踩过的坑、修过的 bug、确认过的医学/工程结论，主动追加进本文件
> （尤其剂量边界、数据结构、平台限制这类容易复发的），不用等用户开口。让后续会话少踩坑。

## ⚠️ 医学安全底线

- 本工具是**剂量参考/核对**用途，不是处方系统。任何方案、剂量、给药频次的新增或修改，
  **必须有指南依据**（优先 **CSCO 指南**，其次药品说明书 / NCCN），并在 commit 说明里标注来源。
- 不臆造剂量。拿不准的数值宁可标注"需核对"，也不要给一个看似合理的数字。
- 涉及剂量边界的改动要格外谨慎（见下方 S-1、卡铂）。

## 数据与计算的关键结构

### 单一数据源
- 全部癌种 / 方案数据在 `miniprogram/utils/chemoData.js`（19 个癌种，约 77 个方案）。
- 方案区分治疗场景用 `setting` 字段（新辅助 / 根治性 / 术后辅助 / 晚期一线 等），
  运行时由 `chemo.js` 拼成 `pickerLabel` 显示。

### 🔴 两份 calc.js 必须同步
公式（BSA / S-1 分档 / 卡铂）有**两份实现**，**改任一份务必同步另一份**，否则两端结果漂移：
- `miniprogram/utils/calc.js` —— 普通页面用，缺参返回 `null`。
- `miniprogram/skills/dose-calculation-skill/utils/calc.js` —— AI skill 用，缺参**抛错** + 严格校验。

两者刻意分开：AI skill 是**独立分包（independent subpackage）**，运行时**不能跨包 require 主包代码**，
所以无法"优雅地"合并成一份。文件头已有同步提醒注释，勿删。

### 核心公式（两份须一致）
- **BSA**（Mosteller）：`√(身高cm × 体重kg / 3600)`
- **卡铂**（Calvert）：`剂量 = AUC × (GFR + 25)`；GFR 用 Cockcroft-Gault，女性 ×0.85，GFR 上限 125。
- **S-1（替吉奥）按 BSA 分档**（按说明书）：`BSA<1.25 → 40mg bid`；`1.25≤BSA<1.5 → 50mg bid`；`BSA≥1.5 → 60mg bid`。
  注意边界 **BSA=1.5 归 60mg bid**（曾因 `<=1.5` 写错过）。

### 方案录入注意
- 不同方案不要合并成一条。例如 **CapeOX ≠ FOLFOX**、**SOX ≠ FOLFOX**——药物组成、给药周期都不同，各自独立成条。
- "A 或 B"型的铂类替代（同一方案内可换药）才允许写在一起，区别于上面这种"两个不同方案被错并"。

## AI Skill 现状（重要）

- AI 剂量计算 skill 代码在分支 **`feat/wechat-ai-dose-calculation`**，**未合并进 main**。
- 原因：微信平台 Skill 能力仍处**灰度测试**，暂不开放提审。带 skill 的版本提审会被拒
  （提示"移除 Skill 相关代码后重新提交"）。
- 因此 **main = 线上生产版本**（不含 skill），等微信开放灰度后再从该分支重新合并提审。
- skill 相关：`miniprogram/skills/`、`miniprogram/AGENTS.md`，以及 `app.json` 的 `subPackages` + `agent` 两段配置。

## 部署

线上即 main 分支内容。发布流程：微信开发者工具打开项目 → 编译预览 → 上传填版本号 → 提交审核。
